// Objeto Math

let resultado;

resultado = Math.PI; //indica el valor de PI
resultado = Math.round(2.5); //Sirve para redondear un número desde .5
resultado = Math.ceil(2.3); //Sirve para redondear hacia arriba desde .1
resultado = Math.floor(2.9); // Es el contrario de .ceil, este redondea hacia abajo
resultado = Math.sqrt(144); // imprime la raiz cuadrada
resultado = Math.abs(-200); // convierte el número en positivo
resultado = Math.min(3,5,7,8,9,10,2);
resultado = Math.max(3,5,7,8,9,10,2);
resultado = Math.random();
resultado = Math.floor(Math.random() * 30);

console.log(resultado)